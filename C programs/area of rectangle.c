#include <stdio.h>
#include <conio.h>
void main()
{
    int l,b,area;
    printf("enter the value of l and b");
    scanf("%d %d",&l,&b);
    area=l*b;
    printf("%d",area);
    getch();
}

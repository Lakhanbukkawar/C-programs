#include <stdio.h>
#include<conio.h>
void main()
{
    int m,a,force;
    printf("enter the mass and the acceleration");
    scanf("%d %d",&m,&a);
    force=m*a;
    printf("%d",force);
    getch();
}
